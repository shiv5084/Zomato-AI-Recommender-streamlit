services:
  - type: web
    name: Zomato-AI-Recommender-backend
    env: python
    plan: free
    region: oregon # Or your preferred Render region
    buildCommand: pip install -r requirements.txt
    startCommand: PYTHONPATH=src python -m uvicorn phase6.api.main:app --host 0.0.0.0 --port $PORT
    healthCheckPath: /api/v1/health
    envVars:
      - key: PYTHON_VERSION
        value: 3.11.8 # Make sure this matches your project's Python version
      - key: GROQ_API_KEY
        sync: false # Set this securely in the Render Dashboard
      - key: CORS_ORIGINS
        sync: false # Set to Vercel origin URLs (comma-separated)
